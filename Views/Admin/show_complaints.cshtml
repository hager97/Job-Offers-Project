@model  List<jobs.Models.ComplaintsViewModel>

@{
    ViewBag.Title = "show_complaints";
    Layout = "~/Views/Shared/_MainLayoutPage.cshtml";
}

<h2>show_complaints</h2>


<table class="table table-striped table-bordered table-hover">
    <tr>
        <th>
            اسم المبلغ عنه
        </th>
        <th>
            الشكاوي
        </th>
        <th>
            الرسالة
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelitem => item.Red_Email)
            </td>
            <td></td>
            <td></td>
            <td>
                @Html.DisplayFor(modelitem => item.Valid)
                @Html.ActionLink("تعديل", "Profile_User", new { Email = item.Red_Email })
                @*@Html.DisplayFor(modelitem => item.Valid)*@
            </td>
        </tr>

        foreach (var subitem in item.Items)
        {
            <tr>
                <td></td>
                <td>
                    @Html.DisplayFor(modelItem => subitem.User.UserName)
                </td>
                <td>
                    @Html.Raw(HttpUtility.HtmlDecode(Html.DisplayFor(modelItem => subitem.Message).ToString()));
                </td>
                <td>
                   
                </td>

            </tr>
        }
    }
    </table>
    <p>
        
        @Html.ActionLink("Back to List", "Index","Home")
    </p>
